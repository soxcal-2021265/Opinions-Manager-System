version: "3.9"

services:
  mongo_db:
    image: mongo:latest
    container_name: opinions_mongo
    restart: always
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db
    environment:
      MONGO_INITDB_DATABASE: opinions_system

volumes:
  mongo_data:
